apiVersion: 1

providers:
  - name: EduPlatform
    orgId: 1
    folder: EduPlatform
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
